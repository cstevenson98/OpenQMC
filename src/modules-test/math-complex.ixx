export module Math.Complex;

import std;

export namespace math::complex {
void print(const std::string& message) {
  std::cout << "Complex module says: " << message << std::endl;
}
}  // namespace math::complex
